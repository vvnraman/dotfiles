#vim ft=hyprlang

# for more configuration options, refer https://wiki.hyprland.org/Hypr-Ecosystem/hypridle
# Chain of events
# - idle -> lock
# - lock -> screen-off
# - screen-off -> suspend
# - suspend -> screen-on
# - screen-on -> unlock

# When closing the lid
# - It triggers suspend via `systemd-logind`
# - suspend locks the screen first

general {
    lock_cmd = ~/.config/hypr/scripts/hyprlock.sh   # avoid starting multiple hyprlock instances.
    before_sleep_cmd = loginctl lock-session            # lock before suspend.
    after_sleep_cmd = hyprctl dispatch dpms on          # to avoid having to press a key twice to turn on the display.
}

listener {
    timeout = 180                                # 2min.
    on-timeout = brightnessctl -s set 10         # set monitor backlight to minimum, avoid 0 on OLED monitor.
    on-resume = brightnessctl -r                 # monitor backlight restore.
}

# turn off keyboard backlight, comment out this section if you dont have a keyboard backlight.
listener {
    timeout = 180                                          # 2min.
    on-timeout = brightnessctl -sd rgb:kbd_backlight set 0 # turn off keyboard backlight.
    on-resume = brightnessctl -rd rgb:kbd_backlight        # turn on keyboard backlight.
}

listener {
    timeout = 300                                 # 5min.
    on-timeout = hyprctl dispatch dpms off        # screen off when timeout has passed.
    # on-resume = hyprctl dispatch dpms on          # screen on when activity is detected after timeout has fired.
    on-resume = hyprctl dispatch dpms on && sleep 1 && hyprctl dispatch dpms on          # double-toggle with delay for amdgpu
}

listener {
    timeout = 1500                                # 25min.
    on-timeout = loginctl lock-session            # lock screen when timeout has passed.
}


listener {
    timeout = 1800                                # 30min.
    on-timeout = systemctl suspend                # suspend pc.
}
